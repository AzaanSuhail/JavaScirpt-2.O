const obj={
    name:'John',
    age:30,
    city:'New York'
}

let entries=Object.entries(obj);
console.log(entries);

let keys=Object.keys(obj);
console.log(keys);

let values=Object.values(obj);
console.log(values);